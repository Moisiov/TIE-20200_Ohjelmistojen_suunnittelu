using System;
using System.Linq;

namespace FJ.Utils
{
    public static class CommonConstants
    {
        public static int[] S_Hours = Enumerable.Range(0, 24).ToArray();
        public static int[] S_Minutes = Enumerable.Range(0, 60).ToArray();
    }
}
